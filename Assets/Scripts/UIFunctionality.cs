using ModularMotion;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIFunctionality : MonoBehaviour
{
    //private void Update()
    //{
    //    if (Input.GetKey(KeyCode.A))
    //    {
    //        if (IsAutoplay && Application.isPlaying && !hasOwner)
    //        {
    //            if (WrapMode == Wrap.Loop)
    //            {
    //                isPlaying = true;
    //            }
    //        }
    //    }
    //}
}
